<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-mybatis.org?DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.mybatis.mapper.Mapper">
    <select id="existTable" resultType="Integer">
        select count(*) from information_schema.TABLES where LCASE(TABLE_NAME="spxsb")
    </select>
    <update id="dropTable">
        drop table if exists spxsb
    </update>
    <update id="createTable">
        create table if not exists spxsb(id int not null  auto_increment,name varchar(100),price float,createDate date,createTime time,primary key(id))
    </update>
</mapper>
